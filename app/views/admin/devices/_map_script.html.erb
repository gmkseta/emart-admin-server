<script src="http://dmaps.daum.net/map_js_init/postcode.v2.js"></script>
<script src="//dapi.kakao.com/v2/maps/sdk.js?appkey=07f10805bc59e2b2e5b4d514a8c69232&libraries=services"></script>
<script>
    var x = document.getElementById('latlng_x').getAttribute('data') * 1
    var y = document.getElementById('latlng_y').getAttribute('data') * 1
    var mapContainer = document.getElementById('map'), // 지도를 표시할 div
        mapOption = {
            center: new daum.maps.LatLng(y, x), // 지도의 중심좌표
            level: 5, // 지도의 확대 레벨
            draggable: false,
            scrollwheel: false,
            disableDoubleClick: false,
            disableDoubleClickZoom: false,
            keyboardShortcuts: false
        };
    //지도를 미리 생성
    var map = new daum.maps.Map(mapContainer, mapOption);
    //주소-좌표 변환 객체를 생성
    var geocoder = new daum.maps.services.Geocoder();
    //마커를 미리 생성
    var marker = new daum.maps.Marker({
        position: new daum.maps.LatLng(y, x),
        map: map
    });
    // 우편번호 찾기 찾기 화면을 넣을 element  
</script>
